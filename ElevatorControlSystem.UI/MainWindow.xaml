<Window x:Class="ElevatorControlSystem.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElevatorControlSystem.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="8" HorizontalAlignment="Left">
            <Button x:Name="StartButton" Content="Start Simulation" Margin="4" Padding="8,4" Click="StartButton_Click" />
            <Button x:Name="StopButton" Content="Stop Simulation" Margin="4" Padding="8,4" Click="StopButton_Click" IsEnabled="False" />
            <TextBlock Text="  " />
            <TextBlock Text="Floor travel (s):" VerticalAlignment="Center" Margin="8,0" />
            <TextBox x:Name="TravelTimeTextBox" Width="40" Text="1" Margin="4,0" />
            <TextBlock Text="Load (s):" VerticalAlignment="Center" Margin="8,0" />
            <TextBox x:Name="LoadTimeTextBox" Width="40" Text="1" Margin="4,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>

            <!-- Visualization area -->
            <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Border BorderBrush="Gray" BorderThickness="1" Padding="8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Column for floor call buttons -->
                        <StackPanel x:Name="FloorCallsPanel" Grid.Column="0" />

                        <!-- Area for shafts -->
                        <StackPanel x:Name="ShaftsPanel" Grid.Column="1" Orientation="Horizontal" />
                    </Grid>
                </Border>
            </ScrollViewer>

            <!-- Side panel: requests and logs -->
            <StackPanel Grid.Column="1" Margin="8" >
                <TextBlock Text="Active Requests" FontWeight="Bold" />
                <ListBox x:Name="RequestsListBox" Height="150" />

                <TextBlock Text="Elevators" FontWeight="Bold" Margin="0,8,0,0" />
                <ListBox x:Name="ElevatorsListBox" Height="150" />

                <TextBlock Text="Log" FontWeight="Bold" Margin="0,8,0,0" />
                <TextBox x:Name="LogTextBox" Height="200" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
            </StackPanel>
        </Grid>
    
    </Grid>
</Window>
